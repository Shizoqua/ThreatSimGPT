# ================================
# Ciicerone - Complete Docker Stack
# ================================
# Production-ready containerized deployment
# Services: PostgreSQL, Redis, Ollama, Ciicerone API, Prometheus, Grafana

services:
  # ================================
  # PostgreSQL Database
  # ================================
  postgres:
    image: postgres:16-alpine
    container_name: ciicerone-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ciicerone}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ciicerone_secure_pass_2024}
      POSTGRES_DB: ${POSTGRES_DB:-ciicerone}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d
    ports:
      - "5433:5432"
    networks:
      - ciicerone-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ciicerone}"]
      interval: 10s
      timeout: 5s
      retries: 5
    labels:
      com.ciicerone.service: "database"
      com.ciicerone.description: "PostgreSQL database for persistent storage"

  # ================================
  # Redis Cache
  # ================================
  redis:
    image: redis:7-alpine
    container_name: ciicerone-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6380:6379"
    networks:
      - ciicerone-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    labels:
      com.ciicerone.service: "cache"
      com.ciicerone.description: "Redis for caching and session management"

  # ================================
  # Ollama - Local LLM Server
  # ================================
  ollama:
    image: ollama/ollama:latest
    container_name: ciicerone-ollama
    restart: unless-stopped
    volumes:
      - ollama_models:/root/.ollama
    ports:
      - "11434:11434"
    networks:
      - ciicerone-network
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_ORIGINS=*
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      com.ciicerone.service: "llm"
      com.ciicerone.description: "Local LLM server with Ollama"
    deploy:
      resources:
        limits:
          memory: 8G
        reservations:
          memory: 4G

  # ================================
  # Ciicerone API Server
  # ================================
  ciicerone-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ciicerone-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama:
        condition: service_started
    environment:
      # Database
      - DATABASE_URL=postgresql://${POSTGRES_USER:-ciicerone}:${POSTGRES_PASSWORD:-ciicerone_secure_pass_2024}@postgres:5432/${POSTGRES_DB:-ciicerone}
      # Redis
      - REDIS_URL=redis://redis:6379/0
      # LLM Providers
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OLLAMA_BASE_URL=http://ollama:11434
      - DEFAULT_LLM_PROVIDER=${DEFAULT_LLM_PROVIDER:-openrouter}
      # Application
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - SECRET_KEY=${SECRET_KEY}
      - ENVIRONMENT=${ENVIRONMENT:-development}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - DEBUG=${DEBUG:-false}
      # Python
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    volumes:
      - ./ciicerone:/app/ciicerone:ro
      - ./templates:/app/templates:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - ./generated_content:/app/generated_content
      - ./reports:/app/reports
    ports:
      - "8001:8000"
    networks:
      - ciicerone-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      com.ciicerone.service: "api"
      com.ciicerone.description: "Ciicerone main API server"

  # ================================
  # Prometheus - Metrics Collection
  # ================================
  prometheus:
    image: prom/prometheus:latest
    container_name: ciicerone-prometheus
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--storage.tsdb.retention.time=30d'
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    ports:
      - "9091:9090"
    networks:
      - ciicerone-network
    depends_on:
      - ciicerone-api
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      com.ciicerone.service: "monitoring"
      com.ciicerone.description: "Prometheus metrics collection"

  # ================================
  # Grafana - Metrics Visualization
  # ================================
  grafana:
    image: grafana/grafana:latest
    container_name: ciicerone-grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin_change_this}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3000
      - GF_INSTALL_PLUGINS=
    volumes:
      - grafana_data:/var/lib/grafana
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./docker/grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3001:3000"
    networks:
      - ciicerone-network
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      com.ciicerone.service: "monitoring"
      com.ciicerone.description: "Grafana metrics dashboard"

  # ================================
  # PgAdmin - Database Management (Optional)
  # ================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: ciicerone-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@ciicerone.local
      PGADMIN_DEFAULT_PASSWORD: admin_change_this
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    ports:
      - "5050:80"
    networks:
      - ciicerone-network
    depends_on:
      - postgres
    profiles:
      - tools
    labels:
      com.ciicerone.service: "tools"
      com.ciicerone.description: "PgAdmin database management interface"

# ================================
# Networks
# ================================
networks:
  ciicerone-network:
    driver: bridge
    name: ciicerone-network
    labels:
      com.ciicerone.network: "main"

# ================================
# Volumes
# ================================
volumes:
  postgres_data:
    name: ciicerone-postgres-data
    labels:
      com.ciicerone.volume: "database"
  
  redis_data:
    name: ciicerone-redis-data
    labels:
      com.ciicerone.volume: "cache"
  
  ollama_models:
    name: ciicerone-ollama-models
    labels:
      com.ciicerone.volume: "models"
  
  prometheus_data:
    name: ciicerone-prometheus-data
    labels:
      com.ciicerone.volume: "metrics"
  
  grafana_data:
    name: ciicerone-grafana-data
    labels:
      com.ciicerone.volume: "dashboards"
  
  pgadmin_data:
    name: ciicerone-pgadmin-data
    labels:
      com.ciicerone.volume: "tools"
